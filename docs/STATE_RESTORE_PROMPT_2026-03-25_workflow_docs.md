# State Restore Prompt (Workflow Docs, 2026-03-25)

1. Load snapshot:
- `docs/SYSTEM_STATE_SNAPSHOT_2026-03-25_workflow_docs.json`

2. Documentation deliverables:
- Added `docs/WORKFLOWS_HOWTO.md` (professional-grade, end-to-end how-to guide).
- Updated `README.md` with a dedicated Workflow Documentation section linking the guide.

3. Scope covered:
- Custom method registration on pretrained models.
- Pretrained load -> prune -> heal -> visualize/save.
- Train baseline -> prune -> heal -> visualize/save.
- Visualization deep dive with artifact persistence.
- GitHub + VS Code + Colab usage assumptions.

4. Continue from here:
- Optional: split this guide into separate per-workflow docs if desired.
- Optional: add screenshot examples and expected artifact tree diagrams.
